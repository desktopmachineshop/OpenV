{
  "name": "openv-e2e",
  "version": "0.0.1",
  "private": true,
  "description": "Playwright E2E smoke pack. Runs against an already-running OpenV stack (BASE_URL, default http://localhost:3000).",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.57.0",
    "playwright": "1.57.0"
  }
}
