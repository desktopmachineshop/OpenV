# Build-time default; 40-openv-api-proxy.sh rewrites this file from
# API_UPSTREAM and /etc/resolv.conf when the container starts.
resolver 127.0.0.11 valid=10s ipv6=on;
set $openv_api_upstream "http://api:8080";
